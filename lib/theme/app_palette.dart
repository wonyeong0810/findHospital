import 'package:flutter/material.dart';

/// Central color tokens for the app's visual language.
class AppPalette {
  const AppPalette._();

  static const brand = Color(0xFF0E7C66);
  static const brandDark = Color(0xFF0A5346);
  static const brandSoft = Color(0xFFDDF0EA);
  static const brandSofter = Color(0xFFF0F7F5);

  static const ink = Color(0xFF13211F);
  static const inkSoft = Color(0xFF5C6B69);
  static const inkFaint = Color(0xFF8A9997);

  static const bg = Color(0xFFEFF3F2);
  static const surface = Color(0xFFFFFFFF);
  static const line = Color(0xFFE5EBE9);

  static const accent = Color(0xFFEFA92C);
  static const accentBg = Color(0xFFFFF3D6);
  static const accentInk = Color(0xFF7A5712);

  static const danger = Color(0xFFCF5B3C);

  static const shadow = Color(0x1A0A2A24);
}
